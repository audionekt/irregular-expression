import { Meta } from '@storybook/blocks';
import { colors, spacing, shadows, radii, duration, easing, fontFamily, fontSize, fontWeight, lineHeight, letterSpacing, breakpoints, zIndex } from '@repo/tokens';

<Meta title="Design System/Tokens" />

<style>{`
  .token-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1rem;
    margin: 2rem 0;
  }

  .token-card {
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    padding: 1rem;
  }

  .color-swatch {
    width: 100%;
    height: 80px;
    border-radius: 6px;
    margin-bottom: 0.5rem;
    border: 1px solid #e5e7eb;
  }

  .shadow-box {
    width: 100%;
    height: 80px;
    background: white;
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #6b7280;
    font-size: 0.875rem;
    font-weight: 500;
  }

  .radius-box {
    width: 100%;
    height: 60px;
    background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
    margin-bottom: 0.5rem;
  }

  .spacing-box {
    height: 40px;
    background: #3b82f6;
    border-radius: 4px;
  }

  .token-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.875rem;
    font-weight: 600;
    color: #374151;
    margin-bottom: 0.25rem;
  }

  .token-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: #6b7280;
  }

  .type-sample {
    margin: 1rem 0;
    padding: 1rem;
    border: 1px solid #e5e7eb;
    border-radius: 6px;
  }

  .table-container {
    overflow-x: auto;
    margin: 2rem 0;
  }

  .token-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.875rem;
  }

  .token-table th {
    text-align: left;
    padding: 0.75rem 1rem;
    background: #f9fafb;
    border-bottom: 2px solid #e5e7eb;
    font-weight: 600;
    color: #374151;
  }

  .token-table td {
    padding: 0.75rem 1rem;
    border-bottom: 1px solid #e5e7eb;
  }

  .token-table code {
    font-family: 'JetBrains Mono', monospace;
    background: #f3f4f6;
    padding: 0.125rem 0.375rem;
    border-radius: 3px;
    font-size: 0.8125rem;
  }
</style>

# Design Tokens

A comprehensive catalog of all design tokens used in the Aurigami design system. These tokens are the building blocks of our UI components.

---

## Colors

### Brand Colors

<div className="token-grid">
  {Object.entries(colors.brand).map(([key, value]) => (
    <div key={key} className="token-card">
      <div className="color-swatch" style={{ backgroundColor: value }} />
      <div className="token-label">brand.{key}</div>
      <div className="token-value">{value}</div>
    </div>
  ))}
</div>

### Gray Scale

<div className="token-grid">
  {Object.entries(colors.gray).map(([key, value]) => (
    <div key={key} className="token-card">
      <div className="color-swatch" style={{ backgroundColor: value }} />
      <div className="token-label">gray.{key}</div>
      <div className="token-value">{value}</div>
    </div>
  ))}
</div>

### Accent Colors

<div className="token-grid">
  {Object.entries(colors.accent).map(([key, value]) => (
    <div key={key} className="token-card">
      <div className="color-swatch" style={{ backgroundColor: value }} />
      <div className="token-label">accent.{key}</div>
      <div className="token-value">{value}</div>
    </div>
  ))}
</div>

### Semantic Colors

<div className="token-grid">
  {Object.entries(colors.semantic).map(([key, value]) => (
    <div key={key} className="token-card">
      <div className="color-swatch" style={{ backgroundColor: value }} />
      <div className="token-label">semantic.{key}</div>
      <div className="token-value">{value}</div>
    </div>
  ))}
</div>

---

## Spacing

<div className="token-grid">
  {Object.entries(spacing).map(([key, value]) => (
    <div key={key} className="token-card">
      <div className="spacing-box" style={{ width: value }} />
      <div className="token-label">spacing.{key}</div>
      <div className="token-value">{value}</div>
    </div>
  ))}
</div>

---

## Shadows

<div className="token-grid">
  {Object.entries(shadows).map(([key, value]) => (
    <div key={key} className="token-card">
      <div className="shadow-box" style={{ boxShadow: value }}>
        {key}
      </div>
      <div className="token-label">shadow.{key}</div>
      <div className="token-value" style={{ fontSize: '0.65rem', wordBreak: 'break-all' }}>
        {value}
      </div>
    </div>
  ))}
</div>

---

## Border Radii

<div className="token-grid">
  {Object.entries(radii).map(([key, value]) => (
    <div key={key} className="token-card">
      <div className="radius-box" style={{ borderRadius: value }} />
      <div className="token-label">radius.{key}</div>
      <div className="token-value">{value}</div>
    </div>
  ))}
</div>

---

## Typography

### Font Families

<div className="table-container">
  <table className="token-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Sample</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(fontFamily).map(([key, value]) => (
        <tr key={key}>
          <td><code>fontFamily.{key}</code></td>
          <td style={{ fontFamily: value.join(', ') }}>
            The quick brown fox jumps over the lazy dog
          </td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

### Font Sizes

<div className="table-container">
  <table className="token-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Value</th>
        <th>Sample</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(fontSize).map(([key, value]) => {
        const size = Array.isArray(value) ? value[0] : value;
        return (
          <tr key={key}>
            <td><code>fontSize.{key}</code></td>
            <td><code>{size}</code></td>
            <td style={{ fontSize: size }}>Sample Text</td>
          </tr>
        );
      })}
    </tbody>
  </table>
</div>

### Font Weights

<div className="table-container">
  <table className="token-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Value</th>
        <th>Sample</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(fontWeight).map(([key, value]) => (
        <tr key={key}>
          <td><code>fontWeight.{key}</code></td>
          <td><code>{value}</code></td>
          <td style={{ fontWeight: value }}>Sample Text</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

---

## Transitions

### Duration

<div className="table-container">
  <table className="token-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(duration).map(([key, value]) => (
        <tr key={key}>
          <td><code>duration.{key}</code></td>
          <td><code>{value}</code></td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

### Easing Functions

<div className="table-container">
  <table className="token-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      {Object.entries(easing).map(([key, value]) => (
        <tr key={key}>
          <td><code>easing.{key}</code></td>
          <td><code>{value}</code></td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

---

## Breakpoints

<div className="table-container">
  <table className="token-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Value</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>breakpoints.xs</code></td>
        <td><code>{breakpoints.xs}</code></td>
        <td>Extra small devices</td>
      </tr>
      <tr>
        <td><code>breakpoints.sm</code></td>
        <td><code>{breakpoints.sm}</code></td>
        <td>Small devices (mobile)</td>
      </tr>
      <tr>
        <td><code>breakpoints.md</code></td>
        <td><code>{breakpoints.md}</code></td>
        <td>Medium devices (tablet)</td>
      </tr>
      <tr>
        <td><code>breakpoints.lg</code></td>
        <td><code>{breakpoints.lg}</code></td>
        <td>Large devices (desktop)</td>
      </tr>
      <tr>
        <td><code>breakpoints.xl</code></td>
        <td><code>{breakpoints.xl}</code></td>
        <td>Extra large devices</td>
      </tr>
      <tr>
        <td><code>breakpoints.2xl</code></td>
        <td><code>{breakpoints['2xl']}</code></td>
        <td>2X large devices</td>
      </tr>
    </tbody>
  </table>
</div>

---

## Z-Index Scale

<div className="table-container">
  <table className="token-table">
    <thead>
      <tr>
        <th>Token</th>
        <th>Value</th>
        <th>Usage</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>zIndex.base</code></td>
        <td><code>{zIndex.base}</code></td>
        <td>Base layer</td>
      </tr>
      <tr>
        <td><code>zIndex.dropdown</code></td>
        <td><code>{zIndex.dropdown}</code></td>
        <td>Dropdown menus</td>
      </tr>
      <tr>
        <td><code>zIndex.sticky</code></td>
        <td><code>{zIndex.sticky}</code></td>
        <td>Sticky elements</td>
      </tr>
      <tr>
        <td><code>zIndex.fixed</code></td>
        <td><code>{zIndex.fixed}</code></td>
        <td>Fixed position elements</td>
      </tr>
      <tr>
        <td><code>zIndex.overlay</code></td>
        <td><code>{zIndex.overlay}</code></td>
        <td>Overlay backgrounds</td>
      </tr>
      <tr>
        <td><code>zIndex.modal</code></td>
        <td><code>{zIndex.modal}</code></td>
        <td>Modal dialogs</td>
      </tr>
      <tr>
        <td><code>zIndex.popover</code></td>
        <td><code>{zIndex.popover}</code></td>
        <td>Popovers</td>
      </tr>
      <tr>
        <td><code>zIndex.tooltip</code></td>
        <td><code>{zIndex.tooltip}</code></td>
        <td>Tooltips</td>
      </tr>
      <tr>
        <td><code>zIndex.toast</code></td>
        <td><code>{zIndex.toast}</code></td>
        <td>Toast notifications</td>
      </tr>
    </tbody>
  </table>
</div>

---

## Usage

Import tokens in your components:

```typescript
import { colors, spacing, shadows } from '@repo/tokens';

// Use in vanilla-extract
import { style } from '@vanilla-extract/css';

export const button = style({
  backgroundColor: colors.brand[600],
  padding: spacing[4],
  boxShadow: shadows.md,
});
```

